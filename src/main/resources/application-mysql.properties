# =====================================
# Informações da aplicação
# =====================================
spring.application.name=spring_proja3
spring.output.ansi.enabled=ALWAYS

# =====================================
# Configuração do banco de dados (MySQL)
# =====================================
spring.datasource.url=${MYSQL_DB_URL}
spring.datasource.username=${MYSQL_DB_USER}
spring.datasource.password=${MYSQL_DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Cria/atualiza automaticamente as tabelas no banco
spring.jpa.hibernate.ddl-auto=update

# Exibir SQL no terminal
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# =====================================
# Configuração do servidor SMTP (Gmail)
# =====================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=contato.eventosgroove@gmail.com
spring.mail.password=jszm xrwa lerh sykm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =====================================
# Recursos estáticos do frontend
# =====================================
spring.web.resources.static-locations=classpath:/static/
